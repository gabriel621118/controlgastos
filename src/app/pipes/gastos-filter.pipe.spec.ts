import { GastosFilterPipe } from './gastos-filter.pipe';

describe('GastosFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GastosFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
